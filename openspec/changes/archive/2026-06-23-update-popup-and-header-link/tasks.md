## 1. Web Application Updates

- [x] 1.1 Update `app/components/ExtensionInstructionModal.vue` to replace the developer setup steps with a prominent Chrome Web Store direct installation button and simplified steps.
- [x] 1.2 Verify `app/components/AppHeader.vue` modal triggering still functions correctly with the simplified modal.

## 2. Chrome Extension Updates

- [x] 2.1 Update `extension/popup.html` structure to add a direct Dashboard link in the header and polish general font styling and spacing.
- [x] 2.2 Update `extension/popup.js` to support opening the user's dashboard link and display a "Voir sur le dashboard" button upon successful job saving.

## 3. Verification

- [x] 3.1 Verify Nuxt frontend builds successfully (`pnpm run build` or `nuxt prepare`).
